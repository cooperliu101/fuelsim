# 首版验收记录（2026-09-18）

- 当前工作区完整 Release 构建成功，`ctest --test-dir build -j8 --output-on-failure`
  通过 399/399 项，外部总耗时 178.57 s。该工作区包含开发前已经存在的其他修改，
  这些修改没有纳入独立温度求解提交。
- 另从原 HEAD 导出干净源码，仅加入本次拟提交改动，完成独立 Release 构建。
  新增单元、状态、原生对比、多进程、参考校验和验证矩阵共 20/20 项通过；
  原有 C3D20T、输入解析及求解器回归另有 3/3 项通过。
- `elements/` 单独配置和构建，四种纯热单元、界面及库边界检查 6/6 项通过。
- 11 个 Abaqus 2025 原生算例全部通过原定 0.5% 非零字段门槛。
  全部逐点指标见 `validation_summary.json`；几何、边界、时间、零字段门槛和
  验证范围见 `README.md`，原生参考字节由 `SHA256SUMS` 保护。
- DCAX4 默认热容采用形函数体积分节点权重；DC3D8 默认热容采用角点配对高斯点
  权重。畸变单元均匀升温比较核对了各节点热反力，而非仅比较总热量。
  DCAX8 与 DC3D20 保持一致热容，二阶中间节点温度参与实际求解。

## 扩展验收（2026-09-19）

- 当前工作区完成 Release 构建，使用 moose 环境编译器、PETSc、ADlite 0.2.3
  和独立 Exodus。构建最多四个作业；生产程序测试限制在 CPU 0—3，所有 CTest
  使用 `-j4`，数值库单线程。
- `ctest --test-dir build -R 'fuelsim_(dcax|dc3d|thermal)' -j4 --output-on-failure`
  通过 34/34 项，耗时 10.45 s。其中 25 项为使用完整静态输入卡的 Abaqus
  原生比较：首版 11 项、温度相关材料及畸变网格 8 项、新增界面 6 项。
- 完整 `ctest --test-dir build -j4 --output-on-failure` 运行全部 414 项，耗时
  247.64 s，413 项通过。唯一失败为四个诊断脚本更新源码读取路径后遗漏了
  SHA256 校验值；更新这四个脚本的校验值后，单独重跑
  `fuelsim_abaqus_reference_sha256` 通过 1/1，耗时 0.99 s。全部 414 项均有
  最终通过结果，没有数值回归失败；这里不将第一次完整运行记为一次性全通过。
- 原生 `.inp`、`.dat`、`.sta` 和导出的节点、积分点参考没有被修改或替换。
  新增六项参考加入纯热校验清单。原始三维二阶界面失败结果保存于
  `dc3d20_contact_before_fix.json`，修正后的全部界面结果见 `contact_summary.json`。
- 比较器反向检查确认：重复原生节点记录会报错；删除最后一个原生时刻会报错。
  温度、温升、热反力与三个热流分量的原有相对或零量绝对门槛均未放宽。
- 独立源码验证发现待提交集合缺少纯热瞬态界面摘要分支：所有时间步完成后，
  摘要错误进入热机械后端而崩溃。已将工作区现有的对应三行修正纳入本次改动，
  保留其他求解器和计时修改在本次提交之外。`dcax8_contact_transient` 的生产
  程序返回码检查覆盖此错误，不能仅凭已有 Exodus 结果标记通过。
- 从原 HEAD 导出源码，只叠加拟提交文件，独立完成相同编译器和依赖的 Release
  构建。补齐摘要分支后，传热、验证矩阵、参考校验和库边界测试 37/37 全部通过，
  耗时 10.30 s。该构建不包含工作区原有的其他求解器、输入或热机械算法修改。
- `elements/` 单独配置并构建，四种纯热单元、二阶界面及库边界测试 6/6
  全部通过，耗时 0.28 s。局部单元库不需要 PETSc、Exodus 或全局核心库。

## 五项验证扩展验收（2026-09-19）

- 新增 21 张完整静态生产输入卡、9 个 Abaqus 2025 原生比较算例，以及
  非匹配/曲面界面、独立物性、磁盘重启、多材料能量平衡和解析收敛检查。
  几何、载荷、指标与适用边界见 `studies.md`。本轮没有修改生产求解算法。
- 工作区 Release 构建成功。完整 `ctest --test-dir build -j4 --output-on-failure`
  运行 418 项，耗时 267.66 s，417 项通过。唯一失败是验证矩阵新增四个条目后，
  审查程序的允许列表和状态计数未同步。补齐允许列表及 195 项已验证条目的计数，
  重新构建后，单独重跑该项通过 1/1，耗时 0.02 s。全部 418 项均有最终通过
  结果；不把首次完整运行记为一次性全通过。
- 新增四组日常验证在完整运行中均通过：原生界面和物性组、磁盘重启组、
  多材料与多进程组、解析代表算例组。原有 25 个与新增 9 个原生算例均通过，
  新增比较的最大逐点相对误差为 0.08794%。所有原有字段门槛保持不变。
- 独立源码使用已提交生产代码，仅叠加本次验证、测试登记和说明文件，排除
  工作区已有的其他求解器改动，完成 Release 构建。纯热、参考校验、验证矩阵
  和单元库边界检查一次通过 41/41，耗时 46.51 s。提交的四组
  `study_*_summary.json` 采用该独立构建的结果。
- 手工执行三档空间和三档时间加密研究，节点温度、积分点热流及总热量的
  完整数值见 `study_convergence_summary.json`。一阶圆筒网格的解析热流误差
  尚未达到 0.5%，报告保留其真实误差，不能将收敛阶通过等同于全字段精度通过。
- 比较器反向检查确认：重复节点记录、删除一个完整参考时刻、将 0.1 s
  原生时刻改成下一个不同单精度数，均会明确失败。支持 ODB 单精度时刻的
  精确对应没有改变唯一性、节点/积分点完整覆盖或物理场误差门槛。
- 所有编译使用最多四个作业，生产与 CTest 运行限制在 CPU 0—3，数值库单线程，
  CTest 使用 `-j4`。Abaqus 参考逐项单核运行，日常测试没有调用 Abaqus。

## 圆筒热流精度研究（2026-09-19）

- 手工运行 `flux_accuracy.py`，使用此前独立源码构建的生产程序完成八组网格
  比较。五个 DCAX4 网格还与闭式离散解析热流核对，最大相对差异低于
  `3.1e-13`，确认主要误差来自常梯度插值。
- DCAX4 的 128 单元均匀网格和 64 单元等比网格，以及 DCAX8 的 8、16 单元
  均匀网格，全部满足此次节点温度、温升、积分点热流及总热反力门槛。
  另外四组保留为未达到热流精度要求的对照，不标记为精度合格。
  具体数值及建议见 `flux_accuracy.md`、`flux_accuracy_summary.json`。
- 原网格生成器的十种已有网格在临时目录重生成后，二十个 Exodus 与 Abaqus
  网格文件全部逐字节一致。原有参考没有改变，新增八个网格文件加入校验清单。
- 相关传热、验证矩阵、参考完整性和库边界检查通过 41/41，耗时 33.94 s，
  使用 `-j4`、CPU 0—3 和单线程数值库。本轮只增加手工精度研究和网格选项，
  未修改 CMake、生产算法或依赖，没有重复运行无关的完整测试套件。
