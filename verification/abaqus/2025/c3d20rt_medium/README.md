# C3D20RT 中等规模圆柱热力接触例题

本例题由已有 B5.56 C3D20T 输入建立独立的 C3D20RT 输入，保留网格、材料、
载荷、时间步和接触参数。参考程序为实际运行的 Abaqus 2025，生产程序为
`fuelsim -i`。这是手动运行的性能和全字段比较例题，不加入轻量自动测试套件。

## 模型和比较范围

| 项目 | 数量或设置 |
|---|---|
| 二十节点体单元 | 1,152 个，燃料 640 个、包壳 512 个 |
| 位移节点 | 5,969 个，每个节点有三个方向的位移 |
| 温度自由度 | 1,617 个，只在单元角点求解 |
| Fuelsim 耦合自由度 | 19,524 个 |
| 活跃材料积分点 | 每个单元 8 个，共 9,216 个 |
| 接触节点 | 燃料外表面 416 个 |
| 时间离散 | 0 至 1 s，共 20 个 0.05 s 增量 |
| 力学与接触 | 有限应变、面到面有限滑移、库仑摩擦系数 0.002 |
| 直接求解器 | Fuelsim 使用 PETSc 提供的 MUMPS；Abaqus 使用自身稀疏直接求解器 |

上述节点和体单元数量指用户网格。Abaqus 另外生成 3,072 个接触内部节点和
1,024 个接触内部单元，原生 `.dat` 记录总变量数为 23,892，包含内部变量。
字段比较覆盖全部用户网格节点和体单元材料积分点，不把这些内部接触对象
重复计入几何节点或体单元数量。

网格直接复用 `verification/moose/m58_integrated_hex20_mesh.e`。燃料高度 5 mm，
外半径 4.12 mm；包壳从 z=-1 mm 延伸至 6 mm，外半径为 4.692 mm。
圆周由平面单元面逼近，中间节点的位置以网格文件为准。

初始温度为 600 K，包壳外表面温度固定为 600 K。燃料体热源基值为
2e8 W/m³，随既有分段线性函数变化，并按时间区间平均施加。
包壳内、外压力基值分别为 0.5 MPa 和 2 MPa；燃料和包壳顶端轴向位移
在 1 s 分别达到 0.6 mm 和 0.02 mm。包壳同时启用 J2 塑性和 Norton 蠕变。
完整参数、底端约束和载荷历史均在独立输入卡中。

Fuelsim 热容遵守固定初始质量规则。Abaqus 原生热离散保留原样，不能为改善
比较结果改变 Fuelsim 的质量契约。2026-09-16 用户明确要求“涉及绝对值很小的
情况，就与绝对容差比较”。本例题据此对位移、机械反力和累计滑移使用下述
绝对容差检查，结果标记为 `qualified`，即按本次约定的规则通过。

## 提交 `149dea5` 的接触优化

1. 主接触面只从最近有效投影所在面向有圆形区域重叠的相邻面扩展。
   原来的全表面扫描会把闭合圆柱背面的投影也计入，从而产生错误的间隙和传热。
   热接触的局部自由度和切线矩阵同步缩小到实际参与的面；全局稀疏矩阵仍为所有
   潜在接触候选保留位置。
2. 八节点曲面投影先用普通双精度求两个局部坐标，再通过投影方程的隐式导数
   求坐标对节点位置的导数。这样避免在每次局部 Newton 迭代中反复计算自动
   微分，也避免毫米尺度曲面上近零导数的消减误差阻止收敛。投影值的收敛要求未放宽。
3. 先用整个约束的平均参考法向确定一个切向，再投影到各个面，并随曲面变形
   传递。这也避免一个面正对全局坐标轴时产生零切向量。圆柱正、负 x 方向
   34 个节点的相邻面原先选择了相反切向方向，平均后轴向滑移丢失。

前两项修改后、第三项修改前的完整计算显示：累计滑移相对 L2 误差为
28.5704%，切向接触力为 14.3941%。该结果用于定位缺陷，不能作为通过验收的依据。
局部回归分别覆盖毫米尺度投影的残量一致性和导数、无关背面不参与接触、
相邻折面上的轴向滑移及其几何导数。

## 本次误差修正

热接触现在分别计算间隙平均和温度平均。有限滑移时，导热律采用温度角点
对应的机械平均间隙，并沿次接触面的当前法向计算；传热面积和温度积分
仍包含原有的边中点采样。两个平均值的面积导数均进入切线矩阵。

另外，Abaqus 的燃料导热系数表按原公式 `3824/T + 0.61` 加密至 660 K，
覆盖本例题的实际温度范围，并已重新计算全部二十帧参考。两张 Fuelsim
输入卡采用更严格的非线性停止条件，以降低自由节点的未平衡力。
固定初始质量、材料参数、载荷与比较门槛均未改变。指定原生节点历程的
隔离诊断、交叉比较及复现命令见 [误差定位与修正](accuracy_refinement/README.md)。

## 比较方法

`extract.py` 从原生 ODB 提取全部二十帧，而不是只取最终帧：

- 节点参考共 119,380 行，包含温度、位移和反力；角点温度与插值温度分别比较。
- 接触参考共 8,320 行，包含间隙、压力、法向力、切向力、累计滑移和传热量。
- 材料点参考共 184,320 行，包含应力、弹性应变、塑性应变、蠕变应变及等效历史。

材料点对应关系仅由 Abaqus 当前节点坐标重建，再与其原生积分点坐标匹配，
逐单元检查一一对应。Abaqus 工程剪应变除以二后与 Fuelsim 张量剪应变比较。
非活跃的 q8 至 q26 输出必须为 NaN。

`compare.py` 只读取生产 Exodus 和参考 CSV，不调用求解器。标量比较整体相对误差（相对 L2，即差值平方和的平方根除以参考值平方和的平方根）、
相对绝对峰值和最大逐点相对误差；三维向量和六分量张量按现有分组指标比较。
标量峰值为两边最大绝对值之差除以最大参考绝对值；向量与张量峰值为最大
差值范数除以最大参考范数，张量使用按六个独立分量排列的欧氏范数。
逐点相对误差不设置分母下限；零参考单独使用脚本中明确列出的绝对容差。
原始存储值全部保留，包括理论约束为零时 Abaqus 输出的极小非零位移。
ODB 帧时间的 5e-8 s 匹配容差仅处理其单精度时间存储，不影响场值门槛。

对位移、机械反力和累计滑移，非零参考的逐点判断采用“相对误差小于 0.1%，
或差值范数小于绝对容差”。沿用这三个字段原有的零参考绝对容差：

| 字段 | 绝对容差 |
|---|---:|
| 位移向量 | 1e-12 m |
| 机械反力向量 | 1e-5 N |
| 累计滑移向量 | 1e-12 m |

通常整体相对 L2 和相对绝对峰值仍须小于 0.1%。如果某组比较的最大参考范数
大于零且小于该字段绝对容差，说明整个参考场均为极小量，此时整体检查改为
最大差值范数小于绝对容差。这样也能检查滑动开始前整帧接近零的累计滑移。
全部零参考仍按原规则检查绝对差；其他字段及其他例题保持各自原验收规则。
各项阈值比较均使用严格小于，等于阈值不算满足该条件。

`comparison.csv` 同时列出所有帧和最终帧的完整指标、最差样本及其参考值；
`coverage.csv` 逐步记录两种非弹性机制是否在同一个材料点同时产生增量。
`comparison_by_frame.csv` 另列每帧指标。原始 `strict_0_1_percent_passed`
和逐点相对误差超限数量均保留，新增 `nonzero_pointwise_absolute_accepted`
记录按绝对容差通过的非零样本数，`near_zero_field_absolute_check` 标明是否
对整个极小参考场采用绝对检查，`qualified_passed` 记录本次规则下的通过状态。
全部帧合并、最终帧和每个单独帧都通过时程序返回 0，任何一项不通过则返回 1。

## 当前完整结果（2026-09-16）

两套程序均完成原定的二十个 0.05 s 时间步。Fuelsim 接受二十步，没有失败重试或
缩小步长。最后一个时间步，两边全部 4,096 个包壳材料积分点都同时产生正的
塑性与蠕变增量；Fuelsim 的 416 个接触节点全部有效，并全部进入摩擦滑动。
完整覆盖检查见 [coverage.csv](coverage.csv)。

**按本次约定的绝对容差规则，全部二十步的 23 个字段通过比较，状态为 qualified。**
全部帧合并与最终帧共 46 条记录通过，逐帧的 460 条记录也全部通过。
原始全字段严格 0.1% 状态仍为未通过；位移、机械反力和累计滑移中的极小
非零参考量保留原始数值、相对误差和原相对判据的失败标记。
下表覆盖全部二十帧，单位为百分数。所有字段与每帧记录分别见
[comparison.csv](comparison.csv) 和 [comparison_by_frame.csv](comparison_by_frame.csv)。

| 字段 | 相对 L2 | 相对绝对峰值 | 最大逐点相对误差 |
|---|---:|---:|---:|
| 角点温度 | 1.137417e-06 | 5.590451e-09 | 1.71646e-05 |
| 位移向量 | 1.304321e-07 | 2.915776e-07 | 100 |
| 机械反力向量 | 7.358941e-06 | 1.409756e-05 | 6163.457 |
| 接触间隙 | 0.0007221072 | 0.0003363186 | 0.004167247 |
| 接触压力 | 0.0007320443 | 0.0003895597 | 0.004125579 |
| 切向接触力 | 0.0006325573 | 0.001662132 | 0.003070998 |
| 累计滑移 | 0.00358274 | 0.004788272 | 100 |
| 界面总传热量 | 3.399406e-05 | 4.139285e-05 | 0.0003956324 |
| 节点热反力 | 0.0007236995 | 0.0005567864 | 0.009821198 |
| 燃料应力张量 | 5.380814e-06 | 9.455746e-06 | 0.0005066703 |
| 包壳应力张量 | 1.3407e-05 | 2.049333e-05 | 9.317471e-05 |
| 包壳塑性应变张量 | 4.656482e-06 | 1.186319e-05 | 7.155613e-05 |
| 包壳蠕变应变张量 | 1.145539e-05 | 1.63904e-05 | 7.671778e-05 |

与基线 `149dea5` 相比，燃料应力、界面总传热量和节点热反力的最大逐点
相对误差分别从 `2.48135%`、`3.35888%`、`6.49552%` 降至 `0.0005066703%`、
`0.0003956324%`、`0.009821198%`。零参考机械反力的最大绝对差
从 `4.87153e-4 N` 降至 `6.552102e-06 N`，已小于原 `1e-5 N` 门槛。

原逐点相对误差超限的数量与绝对尺度如下，这些样本均通过绝对容差检查。
每个样本均仍按原始非零参考计算相对误差，没有设置分母下限，也没有将近零
参考改写为零。

| 字段 | 原相对判据超限的非零样本数 | 这些样本的最大参考范数 | 这些样本的最大绝对差 | 采用的绝对容差 |
|---|---:|---:|---:|---:|
| 位移 | 2560 | 1.177452e-35 m | 1.177452e-35 m | 1e-12 m |
| 机械反力 | 20 | 1.811338e-12 N | 2.848352e-11 N | 1e-5 N |
| 累计滑移 | 1856 | 4.672965e-19 m | 4.672965e-19 m | 1e-12 m |

各字段的零参考绝对误差检查均通过。原逐点相对误差超限详见
[comparison.csv](comparison.csv) 中的 `nonzero_pointwise_failures` 等统计列，
统计条件为原始 `参考范数 > 0` 且 `差值范数 / 参考范数 >= 0.001`。
以上共 4,436 个样本全部通过绝对容差检查，组合判据下的逐点失败数为零。
0.05、0.10、0.15 和 0.20 s 的累计滑移整个参考场均小于 `4.673e-19 m`，
原整体相对 L2 和相对绝对峰值均为 100%。这四帧按最大绝对差小于 `1e-12 m`
通过；其他逐帧整体指标均沿用原相对要求。

收紧停止条件后，完整计算使用 173 次非线性迭代、712 次残量计算和
68 次切线计算。修正代码在原停止条件下仍为 86、106、38 次。
逐步次数见 [solver_iterations.csv](accuracy_refinement/solver_iterations.csv)。
本次改善了误差，但更严格的停止条件增加了求解工作量；运行中与回归任务
共享处理器，因此不以此次外部时间作速度比较。

物理修正提交 `787c49d` 的 Release 完整构建通过，统一回归
**326/326 项通过，159.12 s**，完整记录见
[full_ctest.txt](full_ctest.txt)。回归使用 CPU 1 至 7，与 CPU 0 的生产准确性
计算合计不超过八核，命令如下：

```bash
env OMP_NUM_THREADS=1 OPENBLAS_NUM_THREADS=1 MKL_NUM_THREADS=1 \
  taskset -c 1-7 ctest --test-dir build -j7 --output-on-failure
```

新增局部测试覆盖不同法向与不同间隙平均面积；中心差分方向导数相对误差为
7.92e-11，带导数与不带导数的残量逐项相同，热量与切线各列均守恒。
全局测试覆盖热接触独立启用、角点机械间隙、装配后的几何导数和双进程等价性。
自动测试使用各已有例题自身的验收规则；它不替代本中等规模例题的完整比较。
此前补充原始逐点统计的一致性记录见
[comparison_statistics_check.txt](accuracy_refinement/comparison_statistics_check.txt)。

本次容差修改重新读取既有完整生产结果与原生参考，没有重新求解。46 条汇总记录
和 460 条逐帧记录的所有原有指标均逐字一致，二十帧覆盖记录也逐字节一致，见
[绝对容差复核记录](absolute_tolerance_recheck.txt)。比较器的 30 项边界检查
覆盖绝对阈值两侧与相等情况、零参考、整场极小量、向量差值范数、整体相对
指标超限和未授权字段，见 [检查程序](check_tolerances.py) 与
[检查记录](absolute_tolerance_checks.txt)。运行命令为
`python3 verification/abaqus/2025/c3d20rt_medium/check_tolerances.py`。
更新脚本哈希后的 [文件校验](reference_hash_ctest.txt) 通过。

## 历史接触计时（截至提交 `149dea5`）

原提交 `7a14b9d` 在本中等规模例题的初始接触投影中报错，不能计算有意义的
完整历程加速比，原始输出见 [before_initial_failure.txt](before_initial_failure.txt)。
另用已有六单元首增量例题比较同一构建环境下的旧、新二进制，
完整输入卡和网格逐字复制到隔离目录，保留相同输出。固定 CPU 0、单线程，
两版各预热一次，然后交替测量五次；计时期间没有编译、自动测试或其他求解任务。

| 耗时中位数，秒 | 修改前 | 修改后 |
|---|---:|---:|
| 外部完整进程 | 3.460334 | 2.335864 |
| Fuelsim 内部 | 2.470160 | 1.368581 |

外部耗时减少 **32.50%**，对应 **1.4814 倍**加速，五次均更快。
两版均接受一个增量，执行 14 次非线性迭代、22 次残量计算和 14 次切线计算，
没有缩小步长重试。这是六单元接触首增量的结果，不推广为中等规模加速比。
逐次数据、输入校验和二进制校验分别见 [small_runs.csv](small_runs.csv)、
[small_inputs.sha256](small_inputs.sha256)、[small_executables.txt](small_executables.txt)。

两版结果逐项检查了 1,607 个时间、节点和单元字段数组，有限值与 NaN 位置相同。
最大字段峰值归一化差为 3.89e-9；这是旧、新版本的数值一致性诊断，不替代
Abaqus 的逐点误差验收。完整数据见 [small_field_comparison.csv](small_field_comparison.csv)。

## 历史中等规模单核计时（提交 `149dea5`）

以下记录使用本次精度修正前的源码、求解收敛条件和 Abaqus 导热系数表。
当前输入卡已经更新；重现这组历史时间应使用提交 `149dea5` 的完整输入。
本次没有重新进行独立的单核性能测量。

当时的最终代码、同一台 Intel Core i9-13980HX 计算机、两边各固定一个处理器、关闭
场与历史文件输出，分别独立运行一次。计时期间没有本任务的编译、自动测试或
其他求解进程。逐项记录见 [medium_timing.csv](medium_timing.csv)。

| 程序 | 外部完整进程，秒 | 程序报告的内部时间，秒 |
|---|---:|---:|
| Fuelsim，WSL2，MUMPS | 412.11 | 410.26 |
| Abaqus 2025，Windows 原生 | 405.75 | 401 |

该次历史测量中，Fuelsim 外部耗时比 Abaqus 增加 **1.57%**。
这是一次测量，两边操作系统与运行环境不同；没有验证 Windows 与 WSL 逻辑 CPU
编号对应同一个物理核心，因此不能据此推导其他机器或其他规模的固定加速比例。
Abaqus 原生求解进程的实际处理器掩码为 1，证据见
[abaqus_timing_affinity.txt](abaqus_timing_affinity.txt)。

Fuelsim 共接受 20 步，执行 86 次非线性迭代、106 次残量计算和 38 次切线计算，
没有失败重试。打开和关闭结果输出时，最终区域、接触及求解次数共 18 项汇总值
逐字相同，见 [output_mode_comparison.txt](output_mode_comparison.txt)。
Abaqus 同样在全部二十个 0.05 s 增量后到达 1 s，原生状态记录为
[c3d20rt_medium_friction_timing.sta](c3d20rt_medium_friction_timing.sta)。

Fuelsim 内部时间来自 `total_seconds`，Abaqus 内部时间来自 `.dat` 的
最后一个 `JOB TIME SUMMARY` 中 `WALLCLOCK TIME`。原说明误取前处理的
1 s，本次按分析阶段的最后一项更正为 401 s；外部计时值没有变化。它们各自的计时范围不同，不代替
外部进程计时。带完整字段输出的准确性计算曾与编译或自动测试同时运行，
对应的 `fuelsim_time.txt` 和 `abaqus_time.txt` 只作为运行记录，不用于性能结论。

**速度结论限上述历史版本。** 本次准确性计算包含收紧的非线性停止条件，
并与回归任务共享八核预算，其运行日志不能用于判断当前版本是否快于 Abaqus。

## 复现

在仓库根目录执行生产计算和只读比较：

```bash
env OMP_NUM_THREADS=1 OPENBLAS_NUM_THREADS=1 MKL_NUM_THREADS=1 \
  taskset -c 0 build/fuelsim \
  -i verification/fuelsim/transient_c3d20rt_medium_friction.fsi
python3 verification/abaqus/2025/c3d20rt_medium/compare.py \
  verification/fuelsim/transient_c3d20rt_medium_friction_results.e
```

参考重新计算使用 Windows PowerShell，参数为此目录的 Windows 或 WSL UNC 路径：

```powershell
.\run.ps1 -SourceDirectory <本目录>
```

`-ExistingWork <已完成任务目录>` 先核对输入哈希和原生成功状态，再只执行提取。
日常 CTest 不启动 Abaqus。`SHA256SUMS` 保存输入、脚本和压缩参考的哈希，
`provenance.txt` 保存计算环境与输入来源。

性能计算使用两张另行提交的完整 `*_timing` 输入卡，仅关闭场和历史文件输出，
物理输入及时间步保持一致。分别在没有编译、自动测试或其他求解任务时单独计时，固定一个
处理器并将线程数设为 1。Abaqus 运行命令为 `run.ps1 -Timing`；Fuelsim 使用
`transient_c3d20rt_medium_friction_timing.fsi`。外部总耗时与程序内部分析耗时分开记录。
Windows 原生 Abaqus 与 WSL Fuelsim 的环境不同，单次运行只能代表本机本次测量。
