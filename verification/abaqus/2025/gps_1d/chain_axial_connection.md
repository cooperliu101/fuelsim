# 双切片仅轴向连接修正

2026-09-14 修改既有 `gps_two_slice_chain.inp`，没有增加例题或改变温度、材料和载荷历程。
Abaqus 2025 已重新完成十个增量，生产解析比较和原生比较共 2/2 项通过。
全部正式非零字段三种相对误差的最大值为 5.54181e-11%，原门槛仍为小于 0.1%。
完整指标见 [比较记录](chain_axial_connection_metrics.txt)，原生执行来源见
[gps_two_slice_chain_provenance.txt](gps_two_slice_chain_provenance.txt)。

四个 CAX4T 单元现在各有独立节点，共 16 个节点。公共截面的内体节点
3、4、9、10 仅约束轴向位移相等；外体节点 7、8、13、14 同样仅约束轴向位移。
方程直接引用保留节点 3、7，公共截面的轴向位移仍自由求解。
跨切片不连接径向位移和温度，也不设置端面传热。
本例切片内的径向均匀性由原有给定位移保证，温度仍全部给定为 600 K。

比较程序逐步检查全部 16 个原生节点，新增节点对应各自切片的径向节点，
第二接触面的下端节点更新为 10、13；公共截面四个节点的轴向反力求和。
全部材料点、接触量、截面力及原误差门槛保持覆盖。
Fuelsim 的网格本来只共享轴向控制节点，因此生产输入卡和生产算法无需修改。
该例仍不验证不同切片实际产生不同径向位移或不同温度的加载场景。

2018 目录中的共享网格节点输入与结果保留为历史资料，不代表当前自动测试的连接方式。
