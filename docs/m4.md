# M4 工程数值基础与有限应变

本阶段不增加新的燃料或包壳经验模型，而是提高长工况和后续真实材料关联的
数值可靠性。生产默认仍为 PETSc Newton BASIC 全步；一次 BASIC 求解失败时，
fuelsim 可从该次调用的原始初值自动用 BT 回溯重试，BT 仍失败才交给稳态载荷
二分或瞬态 cutback。输入卡可用 `backtracking_fallback = false` 禁用该恢复层。
两次尝试复用同一 PETSc 工作区，并在输出中记录尝试数、是否使用回退以及
BASIC 的失败类别；SNES 和 KSP 迭代数分别统计。

## AD 活跃的温度相关材料参数

热弹性、Norton 和 J2 参数可选用相对 `reference_temperature` 的线性斜率：

```text
property(T) = property_ref + property_temperature_coefficient
                              * (T - reference_temperature)
```

`E(T)`、`nu(T)`、`alpha(T)`、Norton 的 `A(T)`、`q_ref(T)`、`n(T)`，以及
J2 的屈服应力和硬化模量都直接以 `adlite::Scalar` 求值。塑性、蠕变和耦合
局部隐式根的参数偏导通过隐函数关系回传到温度，因此全局 Jacobian 不丢失
材料参数的温度链式项。运行中若线性关系越过物理定义域会报告 domain error，
由 BT 或 cutback 恢复，不做隐式夹持。

这些线性斜率用于验证 AD 活跃属性接口，不代表任何 UO2 或锆合金标定模型。
当前热应变定义仍为 `alpha(T)*(T-reference_temperature)`；若未来输入平均热
膨胀系数、瞬时热膨胀系数表或经验积分形式，必须作为明确的新材料关联实现。

## 分场残量复核

每次求解记录温度、径向位移和轴向位移三个方程组的初始与最终 L2 范数。
PETSc 给出正收敛原因后，除总残量外还逐场复核绝对门槛或相对降低门槛；因此
热力单位混合不会让某一场在总范数中被完全掩盖。相对降低门槛由
`residual_reduction_tolerance` 控制，默认 `1e-6`。
温度方程和两个力学方程另有默认 `1e-8 W` 与 `1e-4 N` 的绝对门槛；总残量
绝对门槛由三个分场门槛合成。这样不会把瓦特和牛顿强行解释为同一物理尺度，
用户仍应针对具体问题设置有意义的平衡容差。

PETSc 的 `MAX_IT`、`LINE_SEARCH` 或 `LOCAL_MIN` 负停止原因不是自动成功；
fuelsim 会在最终状态重新计算残量，只有总残量和三个分场都满足上述门槛才
接受，并保留原始 PETSc 原因供诊断。自动测试分别覆盖总尺度可接受但温度场
不合格的拒解，以及负停止原因在严格残量复核后被接受的路径。

`field_residual_scaling = true` 可把每一温度行按初始温度残量尺度、两个力学
行按共同初始力学残量尺度缩放，并对残量和 Jacobian 行采用相同因子。该功能
默认关闭：由当前迭代残量自动生成的尺度会随载荷步改变，在 PCMI 接触启动
附近可能改变非线性路径。也可成对设置
`temperature_residual_scale` 与 `mechanical_residual_scale`；求解器分别用其倒数
固定缩放温度行和两个力学行，并跨所有载荷步保持不变。固定尺度与自动缩放
互斥，二者均默认关闭；具体数值必须由工况的物理平衡尺度决定。

## Backward Euler 时间误差控制

`time_error_relative_tolerance > 0` 时启用 step-doubling：从同一完整 committed
状态分别计算一个全步和两个半步，以两个半步解作为接受解。归一化 L2 估计
同时覆盖温度、径向/轴向位移、弹性/塑性/蠕变张量、两个等效应变和应力；
应变类与应力分别使用 `strain_history_time_absolute_tolerance` 和
`stress_history_time_absolute_tolerance`。任一分量估计大于 1 就恢复节点、
时间、载荷、全部积分点历史和已提交应力并缩步重试。误差控制内部的所有求解
继续复用一个 PETSc 工作区；Newton 步长控制使用三次内部求解中的最大迭代数，
而总工作量仍按三次求解累计报告。接受后的热率取两个半步的时间平均，机械功、
弹性能变化和非弹性耗散取两个半步之和，避免把第二个半步的诊断冒充完整接受步。

自动回归包含：8/16/32 径向单元实心圆柱空间收敛，观测阶约 1.77 和 1.80；
Backward Euler step-doubling 相对细步参考将温度误差由约 `9.13e-3` 降至
`1.17e-3`；三档接触罚参数使最大穿透从约 `2.11e-7 m` 单调降至
`6.01e-8 m`。这些是代表性数值证据，不替代每个生产工况自己的网格、时间步
和罚参数敏感性研究。

PCMI 回归证明历史控制不是节点场控制的重复：代表性试验中存在节点误差不超过
1、但材料历史误差超过 1 的拒绝步；最大接受节点估计为 `0.1046`，最大接受
材料历史估计为 `0.9179`，并有 6 次由历史误差触发的拒步。另一个 200 步固定
步长 PCMI 只构造一次 PETSc 工作区；最大相对热平衡
误差为 `6.86e-14`，最大界面热不平衡为 `1.24e-14 W`，最大绝对机械功不平衡
为 `7.54e-14 J`，累计相对机械功不平衡为 `4.31e-7`，塑性和蠕变耗散均非负。
个别近零功步的逐步相对机械比值会等于 1，因为分子、分母都在 `1e-14 J`
量级；验收因此同时使用绝对峰值和全历程累计尺度，不用分母下限制造较小数值。

全局热平衡和机械功平衡由同一份已装配残量、Dirichlet 反力和收敛解分解得到，
主要证明装配—求解—反力记账一致，不是独立物理 oracle。界面热不平衡直接对
每个 STS 局部残量的两侧通量求和，是独立的界面离散守恒证据。塑性/蠕变耗散
当前按参考 RZ 测度积分；小应变下口径一致，有限应变下仅作为非负性和趋势诊断，
不是严格当前构形热力学耗散积分。

M2.3 的 MOOSE 弱式改为与 fuelsim 保守 STS 一致的 mortar gap heat transfer，
同时把验证屈服应力从 `5 MPa` 调整为 `4 MPa`，离开 creep-only/耦合塑性分支
切换面。二者共同形成当前参考，不能把误差下降归因于单一改动。全部节点场、
接触压力、平均状态和 40 个积分点指标均采用并通过统一
`0.1%` 门槛；最差项为塑性应变最大逐点误差 `0.086612%`。

## M4.1 轴对称有限应变几何非线性

每个区域通过必填的 `strain = small|finite` 独立选择运动学。有限应变分支
在参考积分点计算轴对称变形梯度：

```text
Frr = 1 + ur,R      Frz = ur,Z
Fzr = uz,R          Fzz = 1 + uz,Z
Fhoop = 1 + ur/R
```

瞬态步内先由 committed 构形形成增量变形梯度
`Fhat=F_new*inverse(F_old)`。与 MOOSE 默认 `TaylorExpansion` 相同，令
`Cinv_I=inverse(Fhat)*inverse(Fhat)^T-I`，总应变增量取二阶展开：

```text
strain_increment = -0.5*Cinv_I + 0.25*Cinv_I*Cinv_I
```

增量转动使用 MOOSE 的 Rashid 近似。材料在中间构形执行原有 J2/Norton
全隐式返回映射，随后以该转动客观旋转 Cauchy 应力以及弹性、塑性和蠕变
张量历史；等效塑性和等效蠕变标量不变。当前构形梯度由
`F^-T*Grad(N)` 得到，力学残量使用 Cauchy 应力和完整当前测度：

```text
dv_current = 2*pi*(R+ur)*detJ_reference*det(F_rz)*w
```

稳态有限应变没有 committed 积分点历史：每个载荷步都从参考构形
`F_old=I` 对当前总位移做一次 Taylor 更新，载荷步只用于求解延续，不形成
增量材料路径。它对客观刚体转动和共轴总变形自洽，M4.2 也与 MOOSE 的单个
增量一致；但主方向随载荷步旋转的稳态路径不等价于 MOOSE 的增量材料历史，
此类问题应使用 `TransientProblem` 明确推进 committed 状态。

热传导、体热源和 Backward Euler 一致热容仍在参考构形积分。有限应变试探
态若出现 `det(F_rz)<=0`、`Fhoop<=0` 或当前半径非正，会抛出 domain error，
交给 PETSc 线搜索或时间步拒绝机制处理，不做数值夹持。pressure 的构型由边界
上的 `configuration` 选择：参考构形使用参考半径、参考法向和参考表面测度，
当前构形使用当前半径、当前线段法向和当前表面测度，是 follower load。该选择
同时适用于分量 traction。省略 `configuration` 时，小应变自动使用参考构形、有限应变自动使用当前构形；
显式选择非推荐组合会提示但仍按输入执行。

局部证据包括均匀径向/轴向伸长的 Taylor 增量和当前测度解析解、分别隔离
非正平面 Jacobian 与非正当前半径的 domain error、非零 committed 构形及
非零旋转历史上的活跃塑性—蠕变 AD/中心差分切线，以及弹性/塑性/蠕变张量
的独立客观旋转与无迹不变量测试。checkpoint 回归还在 M4.3 非共轴路径的
`2.5 s` 变形态保存并恢复节点、应力和全部张量历史，续算终态与连续 100 步
结果逐分量一致。
`transient_finite_strain_pcmi.fsi` 使用与 M2.3 相同的 53 节点、34 Quad4、
燃料 4 段对包壳 5 段非匹配网格。MOOSE 参考只设置
`strain = FINITE`，故自动选择默认增量有限应变、Taylor 分解和开启的历史
旋转；其余热、接触、材料和 20 个 `1 s` 时间步保持一致。

最终全场三项误差如下，列顺序为相对 L2、相对绝对峰值和最大逐点相对误差：

| 场量 | 相对 L2 | 相对绝对峰值 | 最大逐点相对误差 |
| --- | ---: | ---: | ---: |
| 温度 | 0.00976% | 0.00081% | 0.04262% |
| 径向位移 | 0.08346% | 0.11552% | 0.23659% |
| 轴向位移 | 0.02026% | 0.02409% | 0.06706% |
| 接触压力 | 0.04483% | 0.00811% | 0.09192% |
| 包壳 QP 等效应力 | 0.01157% | 0.03237% | 0.03237% |
| 包壳 QP 等效塑性应变 | 0.13111% | 0.26622% | 0.28277% |
| 包壳 QP 等效蠕变应变 | 0.07113% | 0.18378% | 0.18378% |

最大误差是等效塑性应变的最大逐点相对误差 `0.28277%`，所有指定项均低于
统一的 `0.5%` 门槛。总接触力相对误差为 `0.000663%`，5 个 secondary 节点
均成功投影并处于接触状态。

历史张量客观旋转已有独立局部测试，但 M4.1 PCMI 路径仍接近共轴，其误差
与小应变 M2.3 接近，主要鉴别接触、网格和物理组合，不能单独鉴别或外推
一般非共轴多步大转动运动学。有限应变运动学的主要鉴别证据是局部 oracle、
M4.2 的当前测度/法向/几何刚度和 M4.3 的旋转历史。

## M4.2 当前构形 follower pressure

`Line2RzBoundaryKernel` 的压力分支复用边界相邻 Quad4 的 12 个局部自由度。小应变区域
使用参考半径和参考法向，有限应变区域则在每次残量调用中由当前端点计算
半径以及“法向乘线 Jacobian”，因此压力方向、RZ 周长和表面长度均随位移
更新，ADlite 自动给出完整几何刚度。

局部测试覆盖当前构形合力解析式和 AD/中心差分方向导数。端到端
`steady_finite_follower_pressure.fsi` 在单个 RZ Quad4 外表面施加 `10 GPa`
压力，并与 MOOSE 默认 `ADPressure(use_displaced_mesh=true)` 比较全部 4 个
节点。径向位移三项误差不超过 `2.71e-10%`，轴向位移三项误差不超过
`8.46e-10%`；参考零值节点数量和最大绝对差也单独输出。

同一测试还读取 MOOSE 生成的环形 `2 x 2` 网格，在 `left` 内径和 `top`
端面同时施加 `100 MPa` follower pressure。它专门约束分类边集排序不能改变
父 Quad4 的逆时针法向：全部 9 个节点的径向、轴向位移三项误差分别不超过
`0.0158%` 和 `0.00660%`。两个 M4.2 算例的温度均恒为 `600 K`，只作为
无热负载控制量，不是有限应变热耦合的鉴别证据。

分量牵引可选择 `configuration = current`。它保持用户指定的全局 R 或 Z
方向，但以当前半径和当前 Line2 长度积分，因此具有 ADlite 一致几何刚度；
参考构形模式的几何切线严格为零。局部合力和中心差分 Jacobian 已自动验证，
M4.3 进一步与 MOOSE `ADFunctionNeumannBC(use_displaced_mesh=true)` 端到端
对比。一般随法向或用户向量旋转的 traction law 尚未实现。

## M4.3 非共轴多步有限转动

`transient_noncoaxial_finite_strain.fsi` 使用 MOOSE 生成并追踪的畸变 `2 x 2`
环形 RZ 网格。底边固定，顶边在 100 个 `0.05 s` 步中依次经历轴向拉伸、
径向剪切、轴向反向、剪切反向和再次拉伸。正向剪切阶段 `gamma=1.0`，极分解
转角约 `25.46°`；耦合 J2 塑性和 Norton 蠕变在整个路径中保持活跃。内表面
同时承受线性增长到 `1 MPa` 的当前法向压力，外表面承受线性增长到 `1 MPa`
的当前构形轴向分量牵引。

MOOSE 输入只设置 `strain = FINITE`，不覆盖 Taylor 分解或 Rashid 转动。
由于一个 `ADPressure` 对象只作用于一个位移方程，倾斜内表面的压力用径向和
轴向两个对象共同表示完整当前法向；只保留径向对象会漏掉剪切后的轴向压力
分量。
`fuelsim_m43_noncoaxial_finite_strain_moose_tests` 在每个接受步先对每个单元的
四个积分点按参考 RZ 体积加权平均，再按 `(time, Exodus element id)` 逐单元
比较应力、弹性应变、客观总非弹性应变、等效塑性应变和等效蠕变应变。
因此主验收覆盖 `4 x 100 = 400` 个单元—时间行，而不是跨单元全域平均；终态
另比较全部 9 个节点。节点场和等效历史的三项误差全部低于 `0.5%`；张量
历史的相对 L2 低于 `0.005%`，相对绝对峰值低于 `0.001%`。
本算例同样没有热负载，`600 K` 温度对比是控制量；热接触与热膨胀的端到端
证据来自 M4.1，但其变形路径接近共轴。

循环换向使局部参考张量分量穿过零。修正 MOOSE 压力的轴向分量后，应力的
分量相对 L2 从 `0.004845%` 降为 `0.000525%`。验收按完整轴对称张量的
Frobenius 范数计算，其中 `rz` 剪切分量在张量内积中计两次。应力、弹性应变
和总非弹性应变的相对 L2 分别为 `0.000522%/0.000519%/0.000439%`，相对
绝对峰值为 `0.00104%/0.00144%/0.000667%`，最大逐点相对误差为
`0.00740%/0.00266%/0.01924%`，全部通过统一 `0.5%` 门槛。原分量诊断仍
无分母下限地输出；应力和弹性应变的 `rz` 分量在 `t=2.2 s`、单元 2 达到
`0.5324%`，此处参考应力只有 `1.327 MPa`，绝对差约 `0.0071 MPa`，而全
历史最大应力绝对差为 `0.123 MPa`。

同一测试程序还固化六个定位算例。四单元纯位移路径分别运行弹性、塑性、
蠕变和塑性—蠕变耦合；耦合材料再依次加入完整 follower pressure 和当前
分量 traction。纯位移四个分支的应力相对 L2 均不超过 `0.000560%`；耦合
纯位移、加压力、再加 traction 分别为 `0.000523%/0.000525%/0.000525%`。
纯蠕变路径的应力分量诊断在换向过零处为 `3.6755%`，但完整应力张量的相对
L2、相对绝对峰值和最大逐点相对误差分别为
`0.000354%/0.00115%/0.00272%`，通过统一 `0.5%` 门槛；全历史最大绝对差为
`0.165 MPa`。
为进一步区分本构差异和独立平衡路径差异，MOOSE 另输出全部 9 个节点、100
个时间步的温度和位移。测试从初始态开始逐步把这 900 个节点—时间值送入
`TransientProblem` 的生产 `begin/commit` 事务，再与同一 MOOSE 单元历史
比较。共享状态下应力的相对 L2、相对绝对峰值和最大逐点相对误差分别为
`5.15e-8%`、`3.75e-11%` 和 `0.000602%`；弹性应变、总非弹性应变和等效
蠕变的最大逐点误差均小于 `0.000210%`。三项自动门槛分别为聚合量
`1e-6%`、最大逐点 `0.001%`。

同一共享状态在 fuelsim 弱式上的自由力学残量也逐步重装配。其 L2 和无穷
范数相对局部单元内力尺度的最大值分别为 `0.000923%` 和 `0.001419%`，低于
`0.002%` 门槛；绝对峰值分别为 `14.58 N` 和 `10.22 N`。这说明生产 Norton
更新在相同运动路径上与 MOOSE 一致，原独立求解的 `3.6755%` 分量诊断是两条
极接近的平衡路径在低应力过零时产生的相对误差放大，而不是已复现的本构
错误。因此保留该无分母下限诊断，不修改生产材料算法。
另一个单单元算例把四个节点的位移完全规定，隔离非共轴材料更新：张量最大
逐点相对误差低于 `3.4e-7%`，全套指标中最大的是等效蠕变逐点误差
`0.000265%`。因此本轮修正的是 MOOSE 参考卡，不修改 fuelsim 压力离散或
本构生产代码。

MOOSE 默认只旋转 `stress`、`elastic_strain` 和
`combined_inelastic_strain`；具体径向返回模型输出的 `plastic_strain` 与
`creep_strain` 不随有限转动旋转。因此端到端对标使用客观的总非弹性张量和
两个分机制等效标量，fuelsim 分别旋转的塑性/蠕变张量由独立局部测试验证。
Rashid 近似并非机器精度正交。现在对每个接受步的全部 16 个材料点检查，
fuelsim 塑性和蠕变历史的最大迹漂移分别为 `6.34e-6` 和 `1.46e-7`，自动
门槛为 `<7e-6` 与 `<2e-7`；每一步返回映射增量仍保持无迹。原先基于跨单元
QP0 聚合的迹指标会低估这一局部漂移，已经删除。

这项证据鉴定的是上述畸变四单元、约 `25.5°` 的规定路径，不能外推到任意
转角、任意路径或任意网格。
