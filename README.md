# fuelsim

CAX4T 局部计算现由仓库内的独立[单元库](elements/README.md)提供。该库可仅依赖
ADlite 单独构建和测试；fuelsim 负责全局装配、求解及历史状态的接受与恢复。

轴对称和三维均以 Abaqus 为首要外部参考。轴对称 B7.0—B7.9 使用 CAX4T
四节点温度—位移耦合单元，覆盖小应变、有限应变、热机械接触、蠕变、塑性以及
蠕变与塑性同时作用，所有已定义的非零场三项相对误差均要求小于 0.5%。
算例、逐项结果和适用边界见[轴对称 Abaqus 验证记录](verification/abaqus/b7_rz_validation.md)。
轴对称 MOOSE 对比例题及其专用实现已删除；三维 MOOSE 对比继续保留。
下文 M0—M5 的轴对称 MOOSE 对比描述属于历史记录，不代表当前测试覆盖。

`fuelsim` 是一个依赖精简的 C++17 核燃料性能有限元程序。统一可执行程序
通过输入卡选择 `SteadyProblem` 或 `TransientProblem`，并从一个 Exodus
文件自由组合任意数量的物理区域。稳态问题包含：

- 一个 `.e` 文件中的多个独立 2D 轴对称 RZ Quad4 区域，区域间节点不合并；
- 或一个 `.e` 文件中的多个 3D 笛卡尔 HEX8 区域；不同 block 共用的源节点
  原生映射为一套全局自由度，未共用的区域节点仍保持独立，使用八点全积分、
  小应变或有限应变各向同性热弹性和四字段 `[T, ux, uy, uz]`；
- 或支持接触的 3D HEX20 混合阶区域：温度在八个角点采用一阶插值，三个位移
  在二十个节点采用二阶插值，单元共有 68 个局部自由度；热残差使用 2×2×2
  八点积分，力学残差使用 3×3×3 二十七点积分，力学积分点上的温度由
  HEX8 角点形函数插值；
- 每个区域独立的稳态温度相关热传导、体积热源，以及可选小应变或有限应变
  热弹性；
- 由 `primary`、`secondary` 边集定义的气隙导热；轴对称可选 NTS（节点到面）
  或 STS（面到面），三维使用 STS；
- secondary 节点到 primary 线段的唯一 NTS 法向接触，可选显式或材料—网格
  自动罚刚度、事务化增广拉格朗日乘子，以及具有粘着、滑移和已提交切向历史
  的 Coulomb 摩擦；
- 机械接触按当前构形在完整 primary 开放边链上动态选择唯一线段，并在初始
  稀疏结构中预留全部潜在耦合；
- ADlite 生成体单元和界面的局部 Jacobian；
- PETSc SNES、KSP 和分布式 AIJ 稀疏矩阵完成 Newton 求解；局部贡献按 MPI
  rank 唯一分区装配，回调只收集本地贡献及接触搜索依赖的影子自由度。

瞬态问题在同一接触离散上实现：

- M2.1：一致热容矩阵、Backward Euler 物理时间积分、热源斜坡、
  committed/trial/commit/rollback 和失败步 cutback；
- M2.2：通用 J2 Norton 蠕变、J2 线性各向同性硬化塑性及同一
  材料点的全隐式耦合；
- M2.3：非匹配芯块—包壳网格上，芯块热膨胀闭合初始间隙的 PCMI 回归，
  包壳同时累积塑性与蠕变历史，并与同网格、同时间步 MOOSE 算例比较；
- B3.2：三维 HEX8 使用完整 `3×3` 增量变形梯度、MOOSE 默认 Taylor 应变、
  Rashid 转动、六分量客观历史和当前构形力学弱式；塑性、蠕变及其全隐式
  耦合均在同一追踪网格和十步加载路径上通过 MOOSE 三项误差比较；
- B3.8—B4.2：三维 HEX8 可显式选择 Abaqus 风格的表面到面罚接触。
  每个四节点面采用四个节点中心平均约束，并守恒投影到匹配或非匹配 primary 面；
  匹配、双向非匹配、倾斜初始间隙和双切向 Coulomb 摩擦均与 Abaqus/Standard 对比，
  小滑移固定参考支持；有限滑移在当前构形中跨面搜索并允许小应变或有限应变区域，
  已用 20 个匹配增量对比 0.35 弧度法向旋转下的双切向滑动和反向再粘着。
  全部场量三项误差低于 `1%`，并覆盖局部切线、客观双切向历史、历史事务、检查点和
  两进程装配；
- B6：HEX20-U2/T1 支持稳态和瞬态、小应变和有限应变、塑性与蠕变
  历史、二次面压力和牵引、一阶温度对流、对流面 2×2 积分与力学面 3×3 积分、
  二次面热接触、默认表面到面罚函数机械接触、非匹配多面投影、积分点 Coulomb 摩擦、
  Exodus 结果及检查点重启动；四个 primary 面对两个 secondary 面的非匹配压缩例题使用
  同一 88 节点、6 单元网格与 Abaqus/Standard 对比法向位移、十三个节点压力和合力；
  无摩擦小应变路径还使用 Abaqus 对标识别的八个节点中心平均约束，并同时通过平面面片
  拼成的圆柱和面内法向连续变化的真正二次圆柱全场对比；
  正集总和有符号一致节点面积只作为显式节点到面对比选项保留；
- M4.1：与 MOOSE 默认一致的增量 Taylor 有限应变、Rashid 增量转动、历史
  张量客观旋转和当前构形力学弱式；非匹配有限应变 PCMI 的温度、位移、
  压力、应力及非弹性历史均与 MOOSE 逐点比较；
- M4.2：有限应变区域的压力使用当前半径和当前法向形成 follower load，
  并以 10 GPa 单元级 RZ 算例对比 MOOSE 默认 `ADPressure`；
- M4.3：畸变四单元上的 100 步非共轴拉伸—剪切—反向路径达到约 `25.5°`
  转动，并含当前构形压力和分量牵引；逐单元、逐时间步比较 MOOSE 的应力、
  弹性应变、客观总非弹性应变及塑性/蠕变等效历史，并用材料分支、载荷分支
  和单单元全位移控制算例隔离误差来源；
- 燃料、包壳各自独立的积分点 `double` 历史，Newton 回调内只生成
  ADlite trial 状态；
- 瞬态热传导与准静态力学耦合，PETSc 工作区跨时间步复用。
- 版本化二进制检查点保存完整 committed 状态、下一控制步长和最后守恒摘要，
  并通过模型签名、字节序、长度和校验和执行严格重启动；
- Exodus 场结果包含节点温度/位移/接触量、四积分点应力与非弹性历史，以及
  守恒界面总量。
- 严格分段线性时间表可驱动热源、压力、牵引、约束和对流参数，时间推进会
  精确命中表节点；对流边界具有 ADlite 一致局部切线。
- 非线性迭代窗口可自适应调节时间步，拒绝尝试和终止原因结构化记录；
  缩步后的下一成功步不会立即放大回失败尺度；时间误差控制默认关闭并有明确
  启用指导和 100 秒 PCMI 全局收敛证据；
  `--check-jacobian` 提供温度/径向/轴向分场的全局方向导数检查；
- 默认 BASIC Newton 失败后可从原始初值自动用 BT 重试；最终残量按温度、
  径向和轴向方程分场复核；可选固定物理残量尺度，Backward Euler
  step-doubling 同时控制节点场、应力和全部非弹性历史；SNES 与 KSP 工作量
  分别记录；
- 每个接受步记录全局热平衡、界面热不平衡、机械功平衡、弹性能变化及
  塑性/蠕变耗散；200 步 PCMI 回归检查这些诊断并保持单一 PETSc 工作区；
- 热弹性、Norton 和 J2 的线性温度系数以 ADlite 活跃量求值并保留温度链式
  切线；空间、时间步和接触罚参数已有代表性自动收敛证据；
- M3.3：一般 RZ Line2 法向接触、非匹配 STS 分片积分和多芯块端面 NTS。

M2 材料参数仅用于算法和软件验证，不代表真实燃料或包壳经验模型。
具体状态契约、算法和边界见 [M2 设计说明](docs/m2.md)。
坐标与张量约定、体单元和界面弱式、接触与时间离散、求解器结构及验证证据
的统一索引见 [理论、离散与验证手册](docs/theory.md)。
阶段 B 三维热力耦合已经实现；范围包括稳态与瞬态 HEX8 小应变和有限应变
热弹性、瞬态塑性、蠕变及其全隐式耦合、当前构形热接触、节点到四节点面的罚函数
机械接触、Abaqus 风格小滑移与有限滑移表面到面罚接触、三维 Coulomb 摩擦、三维
Exodus、结果和检查点，以及单进程与多进程装配。HEX8 有限滑移表面接触可用于小应变
或有限应变区域；三维增广拉格朗日接触仍未实现。具体后端边界、分阶段范围和验收证据见
[三维热力耦合重构参考](docs/3d-thermomechanics-refactor.md)。

三维 HEX8 区域还可以显式选择 Abaqus `C3D8RT` 风格的一点减缩积分热力耦合
公式，并支持小应变和有限应变。有限应变路径使用当前构形平均梯度与体积、
中间构形 Hughes-Winget 客观更新，以及由总沙漏能量一致求导得到的空间力和
Jacobian。力学沙漏控制固定使用 Abaqus 默认的 `0.005 × 初始剪切模量` 总刚度
算法，热稳定化只由单元几何推导；没有为降低误差而设置的拟合系数。规则和畸变
算子、热容、热载荷、瞬态完整场、非弹性局部切线、事务和检查点均有自动测试。

M0 单燃料圆柱仍作为解析解和 MOOSE 回归基线保留。项目不定义自己的
C++ 模板，不引入 Eigen、Boost、JSON/YAML、日志库或第三方测试框架。
除 ADlite 外，PETSc 是唯一直接外部数值依赖，Exodus 是唯一直接网格 I/O
依赖；MPI、BLAS 和直接求解器等只允许作为 PETSc 的传递依赖。

## 构建

完整的换机复现步骤、固定版本、依赖校验和持续集成执行器设置见
[可复现构建与持续集成](docs/reproducible-build.md)。固定的 Linux 构建环境由
`dependencies/moose-2026.06.16-linux-64.yml` 创建；ADlite 和 Exodus 分别由
`scripts/install_adlite.sh` 与 `scripts/build_exodus.sh` 安装到持久前缀。

提交代码前可安装仓库追踪的格式化钩子：

```bash
scripts/install_git_hooks.sh /home/cooper/miniforge/envs/moose/bin/clang-format
```

此命令把当前仓库的 `core.hooksPath` 指向 `.githooks`。此后执行 `git commit`
时，提交前钩子会对已暂存的 C/C++ 文件运行仓库的 `clang-format` 配置并重新
暂存格式化结果。如果同一文件还包含未暂存改动，钩子会停止提交，避免把这些
改动意外加入提交。其他克隆需要各自运行一次安装命令。

在已经完成依赖安装并激活固定 MOOSE 环境后，设置两个任务专用路径：

```bash
fuelsim_toolchain_prefix="${CONDA_PREFIX}"
fuelsim_dependency_root="$(cd .. && pwd)/fuelsim-dependencies"
```

### 主开发入口：旧 PETSc + 直接 Exodus API

PETSc 只负责求解，不需要启用 Exodus。Exodus 单独构建为串行 I/O 库并复用
固定 MOOSE Conda 环境中的 NetCDF；fuelsim 不使用 DMPlex，也不使用
`PetscViewerExodusII`。固定的 ADlite 安装脚本和 fuelsim Release 配置都默认
启用链接期跨翻译单元优化；性能验收要求保留该设置。使用该环境中的 PETSc
构建 fuelsim：

```bash
env \
  PATH="${fuelsim_toolchain_prefix}/bin:/usr/local/bin:/usr/bin:/bin" \
  PKG_CONFIG_PATH="${fuelsim_toolchain_prefix}/lib/pkgconfig" \
  cmake -S . -B build \
    -DCMAKE_BUILD_TYPE=Release \
    -DCMAKE_CXX_COMPILER="${fuelsim_toolchain_prefix}/bin/c++" \
    -DCMAKE_PREFIX_PATH="${fuelsim_dependency_root}/adlite-0.2.3" \
    -DSEACASExodus_DIR="${fuelsim_dependency_root}/exodus-2024-06-27/lib/cmake/SEACASExodus"

env PATH="${fuelsim_toolchain_prefix}/bin:/usr/local/bin:/usr/bin:/bin" \
  cmake --build build --parallel 4

ctest --test-dir build -j4 --output-on-failure
```

CTest 只保留一个最多四并发的统一回归入口。扩展参数扫描和大型性能路径作为手动
验证资料保留，不再用标签形成第二套测试层级。端到端生产算例与内部数值契约
测试的入口边界见 [测试入口说明](tests/README.md)。

`fuelsim_io` 将一个二维非结构 Quad4 或三维 HEX8 文件的节点、连接关系、元素块、
节点集和边集转换为 fuelsim 自有网格对象；专项 CTest 通过 Exodus API
写出并逐项回读。每个 `[Regions/<name>]` 选择一个元素块；接触不再重复声明
块名，而是由边集相邻单元自动确定所属区域。这样同一入口既能分析单独芯块
或包壳，也能组合芯块—包壳以及芯块—包壳1—包壳2。所有 MOOSE 对比都会
读取仓库内由对应 MOOSE 输入生成的 `*_mesh.e`。一般非结构 Quad4 可进入
I/O 层，生产问题会保留每个选中块的原始节点坐标和 Quad4 连接关系，不再
重建张量积 RZ 网格。单元仍须具有有效的正 Jacobian。接触边界可以是圆柱
侧面、水平芯块端面或斜面，并支持非匹配 Line2 分段；每侧必须是一条不分叉
的开放边链，且从面投影须被主面完整覆盖。直接 Exodus I/O 仍是串行 API，
当前由每个 rank 独立读取同一文件；问题几何和 committed 状态仍在各 rank
复制，但每次 PETSc 残量/Jacobian 回调只收集该 rank 贡献和接触搜索依赖
所需的影子自由度。
Vec、Mat、SNES、贡献计算和线性求解均为真实分布式对象。

运行稳态燃料—包壳工况：

```bash
./build/fuelsim -i verification/fuelsim/steady_b78_rz_small_contact.fsi
```

运行带包壳塑性—蠕变耦合的瞬态 PCMI 工况：

```bash
./build/fuelsim -i \
  verification/fuelsim/transient_b13_small_cax4t.fsi
```

输入卡采用严格、带版本号的 MOOSE 风格分段文本；未知段、未知键、重复键和
不适用于所选本构模型的参数都会立即报错。网格尺寸和几何只从 Exodus 文件
读取，不在输入卡中重复维护。完整字段说明见
[输入卡说明](docs/input-card.md)。

瞬态工程输出示例：

```text
[Executioner]
  type = transient
  # 其余时间推进字段省略
  restart = previous.checkpoint
[]

[Outputs]
  exodus = fields.e
  checkpoint = latest.checkpoint
  checkpoint_interval = 5
[]
```

检查点只包含最后一个成功提交态及其控制步长、守恒摘要，不包含 Newton trial
或失败时间步。严格
状态与文件契约见 [M3 工程化说明](docs/m3.md)。工程证据等级、自动审计入口
和当前不支持范围见 [工程验证矩阵](docs/verification.md)。

运行轴对称 Abaqus 生产验收：

```bash
ctest --test-dir build -j4 -R 'b13_|b7[0-9]_rz_' --output-on-failure
```

CTest 运行完整生产输入卡，再读取结果与独立参考比较。轴对称 MOOSE 的旧验收
入口已删除。PETSc 选项仍可在命令行追加，例如 `-snes_monitor`。

多 rank 默认使用 PETSc 并行 MUMPS 直接分解，单 rank 可用
`direct_factorization = mumps` 明确选择同一分解器；经过具体算例计时后可用
`mumps_ordering = scotch|pord` 选择排序算法，默认使用 PORD，已通过重复符号分解时的
重启动逐位一致性检查。
MUMPS 配合 PORD 时会为直接分解另建等值矩阵，删除严格为零的非对角元素，并在
每次矩阵更新时重建符号分解；完整接触候选仍保留在装配矩阵中。显式选择 SCOTCH 或
其他 PETSc 排序时保留完整矩阵分解路径。也可在 `[Solver]` 选择
`gmres` 与 `block_jacobi`、`field_split` 或 `hypre`。例如：

```bash
mpiexec -n 2 ./build/fuelsim \
  -i verification/fuelsim/steady_b78_rz_small_contact.fsi
```

只有 rank 0 写 console、CSV、Exodus 和 checkpoint，避免并行文件竞争。

程序会同时输出问题构造、PETSc 设置、非线性求解、残量回调和 Jacobian
回调的内部计时。20 个载荷步复用同一问题几何、SNES、Vec、Mat、矩阵非零
结构和回调缓冲区；热源只更新各区域的具体核参数。

M2 同样复用几何和 PETSc 工作区，但每个成功时间步会提交 nodal 温度/位移
以及积分点非弹性历史。失败尝试从最后一个 committed 状态重启，不会把
未收敛 Newton 状态作为下一次初值。

## 数值契约

全局状态采用统一的 field-major 排列：

```text
[T(:), ur(:), uz(:)]
```

每个体单元及界面贡献均固定为 12 个局部自由度，因此 PETSc 只保留一条
装配路径。体单元顺序为：

```text
[T0..T3, ur0..ur3, uz0..uz3]
```

热界面的四个节点按
`[secondary0, secondary1, primary0, primary1]` 排列，对应：

```text
[Ts0, Ts1, Tp0, Tp1,
 urs0, urs1, urp0, urp1,
 uzs0, uzs1, uzp0, uzp1]
```

当前间隙统一由当前轴对称 RZ 几何计算，界面定律为：

```text
g = (x_primary - x_secondary) . n_primary_current
h = gap_conductivity / max(g, minimum_gap)
q = h * (Ts - Tp)
p = contact_penalty * max(-g, 0)
```

热接触也可显式选择用于 Abaqus 对标的线性仿射定律：

```text
T_average = (Ts + Tp) / 2
h = conductance
  + clearance_derivative * g
  + pressure_derivative * p
  + temperature_derivative * (T_average - reference_temperature)
q = h * (Ts - Tp)
```

它复现 Abaqus 间隙导热表一个线性单元内对间隙、压力和平均温度的响应与导数。
任意表格的分段插值、外推和截断规则尚未实现，因此不能把当前验证外推到完整
Abaqus 表格语义。试探态导热系数为负或非有限值时会明确拒绝该状态。

其中 `n_primary_current` 从 secondary 指向 primary。圆柱侧面、水平端面和
斜面使用同一套投影、当前法向和轴对称面积公式；竖直圆柱面才自然退化为
`g=(Rp+urp)-(Rs+urs)`。`q>0` 表示热量由 secondary 流向 primary，`p>0`
表示压缩接触压力。
罚形式使用上式；增广拉格朗日形式使用
`p=max(lambda-contact_penalty*g,0)`，并在收敛 Newton 解上更新非负乘子
`lambda=max(0,lambda-contact_penalty*g)`。`contact_penalty` 的单位为
`Pa/m`，也可由两侧杨氏模量与法向网格尺度自动计算。热接触的参考重叠分片只
用于生成 secondary 侧积分点；构造期为每个积分点与完整 primary 链的每条线段
预留稀疏候选，状态验证时在当前构形选择唯一活动段。内部顶点使用半开区间唯一
归属，积分点滑出完整链时明确拒绝当前状态，不夹持到链端或使用负形函数外插。
热流在当前 secondary 表面 `2*pi*r*J` 上积分，并把严格相反的热流投影到
primary 节点。每个 secondary
节点只有一个有效机械投影，节点反力按当前 secondary 半边面积集总后，通过
primary 线段形函数分配相反反力。两种界面残量均离散守恒，投影、面积和
界面定律都由 ADlite 线性化。

默认燃料高度为 `10.000 mm`，包壳高度为 `10.020 mm`。顶部额外的
`20 um` 轴向裕量用于防止燃料热膨胀后越过包壳接触面。

PETSc Dirichlet 行采用 `F_i=x_i-g_i`，Jacobian 只清约束行并置单位对角。
默认使用 Newton BASIC 全步；一次 BASIC 失败会从该调用的原始初值自动用
BT 重试，仍失败才由载荷步或时间步 cutback 恢复。正收敛原因仍须通过总残量
及温度、径向、轴向三个分场残量复核。

M2 热容残量和非弹性更新为：

```text
Rcap_i = integral(N_i*rho*cp*(Tnew-Told)/dt*2*pi*r*dA)
q_creep + 3*G*dt*A*(q_creep/q_ref)^n = q_trial
delta_ep = max((q_trial-yield_old)/(3*G+H), 0)

q_trial = q + 3*G*(delta_ep+delta_ec)
delta_ec = dt*A*(q/q_ref)^n
q = yield_old + H*delta_ep               coupled active branch
```

材料点可选择 `elastic`、`norton_creep`、`j2_plasticity` 或
`norton_creep_j2_plasticity`。耦合分支先判断蠕变松弛后是否仍然超出
屈服面，再同时求解塑性和蠕变增量。

## 当前验收

CTest 覆盖：

- 严格输入语法、物理问题调度、未知键和本构条件字段拒绝；
- 单区域、三层区域和两对 `primary/secondary` 接触的自由组合；
- 双芯块 4 对 6 非匹配端面 STS/NTS 接触以及轴线端点面积与所有权；
- 稳态与瞬态输入卡读取 MOOSE Exodus 网格的端到端求解；
- RZ 体积积分、形函数和梯度恒等式；
- 体单元 AD Jacobian 与中心差分方向导数；
- 有限应变均匀伸长的 Taylor 增量/当前测度解析解、正 Jacobian 守卫，以及
  弹性和活跃塑性—蠕变分支的 AD Jacobian；
- 弹性/塑性/蠕变历史的客观旋转，以及 follower pressure/current traction
  当前构形合力、AD Jacobian 和 MOOSE 对比；
- 开放、最小热隙饱和和闭合接触三种界面分支的 AD Jacobian；
- STS/NTS 热流和 NTS 节点反力守恒；轴对称四类单元的 NTS 原生探测通过
  0.1% 门槛，范围见 [B14 接触验证](verification/abaqus/B14_NTS.md)；
- 接触投影半开区间的唯一性、NTS 端段延伸投影、内部顶点滑移不双计、跨多段
  动态所有权转移、完整 primary 链越界拒步和加高包壳覆盖；
- 同一热接触参考分片的两个积分点可在当前构形分别选择不同 primary 线段，
  只有唯一活动候选计热且两侧热残量严格守恒；
- 带热源实心圆柱解析温度；
- 无应力自由热膨胀；
- 开口端 Lamé 厚壁圆筒压力解；
- 开放气隙燃料—包壳圆柱的解析热阻解；
- 闭合气隙的 20 步端到端求解；
- 匹配物理与网格设置的 MOOSE 全场温度、径向/轴向位移、接触压力分布及
  总反力对比；非零参考场统一检查相对 L2、相对绝对峰值和最大逐点相对
  误差；
- M0、M1、M2.1、M2.2 四条材料路径和 M2.3 PCMI 全部直接读取各自 MOOSE
  输入生成的 Exodus 网格，核对块、节点集和边集后再执行对比；
- M2 瞬态体单元 AD Jacobian、一致热容矩阵、均匀绝热升温解析解，以及
  非均匀瞬态热传导的二阶空间和一阶时间制造解；
- J2 塑性闭式径向返回、卸载和活跃分支 AD 切线，以及逐时间步
  MOOSE 卸载—再加载对比；
- Norton `n=1` 解析根、非线性局部残量和活跃分支 AD 切线；
- 塑性—蠕变耦合应力平衡、屈服一致性、退化分支、极端尺度和 AD 切线；
- M2 committed 状态不受残量/Jacobian 回调影响，接受步提交且拒绝步回滚；
- 两步 M2 PETSc 工作区复用、20 步同点非零蠕变/塑性演示，以及 200 步 PCMI
  的固定残量尺度、热/力守恒和耗散诊断；
- MOOSE 瞬态热容、Norton、J2 单调与卸载—再加载路径及两套耦合载荷路径参考。
- 芯块热膨胀闭合 1 um 间隙、5 个节点接触、包壳塑性—蠕变耦合的
  20 步 PCMI—MOOSE 端到端回归，全部节点、压力和 40 个积分点指标低于
  `0.1%`。
- 相同非匹配网格和载荷路径上的有限应变 PCMI—MOOSE 逐节点/逐积分点回归，
  所有指定三项误差均低于 `0.5%`。

独立 MOOSE 输入、结果快照和运行条件位于
`verification/moose/`。

默认 M1 最终步对全部 528 个节点以及 11 个燃料接触节点进行比较。温度、
径向位移、轴向位移和接触压力的相对 L2 误差分别为 `0.00685%`、
`0.04524%`、`0.01710%` 和 `0.14581%`；相对绝对峰值误差分别为
`0.00117%`、`0.08968%`、`0.02347%` 和 `0.21581%`；最大逐点相对误差
分别为 `0.05813%`、`0.26877%`、`0.76044%` 和 `0.21581%`，全部低于统一
`1%` 门槛。轴向最大逐点指标位于非零参考值约 `0.1436 um` 处，绝对差为
`1.092 nm`，没有使用分母下限。总接触反力的三项单值误差为 `0.05237%`，
11 个燃料表面节点均成功投影且处于接触状态。

内部节点畸变的非张量 M1 网格保留全部原始 Exodus 连接关系。与 MOOSE
逐节点全场比较时，温度、径向位移、轴向位移和接触压力的相对 L2 误差分别
为 `0.00686%`、`0.04532%`、`0.01711%`、`0.14357%`；同一顺序下，相对
绝对峰值误差分别为 `0.00106%`、`0.08964%`、
`0.02347%`、`0.21312%`，最大逐点相对误差分别为 `0.05805%`、
`0.26863%`、`0.76783%`、`0.21312%`，全部低于统一 `1%` 门槛。轴向最大
逐点绝对差为 `1.095 nm`。逐点相对误差不对精确零参考值做除法；径向和轴向位移分别有 11 和
48 个零参考点，其最大绝对差均为 `0`。

参考输入的 `quadrature = true` 会在 MOOSE 中为 fuel 和 cladding 两侧分别建立
独立的 `GapHeatTransfer` 边界条件。用 `save_in` 重放最终步得到 fuel 侧
`106.6533015 W`、cladding 侧 `106.7953395 W`，相差 `0.1420380 W`，约占界面
热率的 `0.133%`。fuelsim 使用同一份 secondary 积分热流施加等量反向反力，
继续保持严格离散守恒；这一参考不平衡只作为剩余场差的边界说明，不复制进
生产公式。

M2 的 MOOSE 最小参考同样比较全部网格节点的三个场。均匀瞬态升温的温度
三项相对误差均为 `0`；径向和轴向位移参考场全为零，三项绝对误差也均为
`0`。非均匀制造解在 `4/8/16/32` 个径向单元上得到 `1.981/1.996/2.000`
的相邻空间收敛阶，在 `0.2/0.1/0.05/0.025 s` 时间步上得到
`0.978/0.991/0.996` 的相邻时间收敛阶；参照温差分别为 `50 K` 和 `10 K`。
Norton 的应力、
等效蠕变和位移误差分别为 `0.0019924%`、`0.0089635%` 和
`0.0036523%`；J2 应力和等效塑性应变与解析值在输出精度内一致。J2
卸载—再加载路径逐步比较 30 个接受步，六个历史量的三项误差最大为
`2.38e-12%`；在 `2 s` 卸载点，应力为 `-98.02 MPa`，等效塑性应变相对
`1 s` 峰值不变，随后在 `3 s` 增加到 `1.48515e-3`。耦合
牵引路径的应力、等效塑性、等效蠕变和位移误差分别为
`0.00000394%`、`0.001188%`、`0.000472%` 和 `0.000408%`；位移控制路径
四项误差也均小于 `0.001%`。

M2.3 PCMI 算例采用弹性芯块和耦合 Norton—J2 包壳，接触界面为燃料 4 段
对包壳 5 段的非匹配网格。20 s 末，5 个芯块表面节点均投影并接触。全部
53 个节点的温度三项误差最大为 `0.010378%`，径向和轴向位移分别最大为
`0.070356%` 与 `0.029326%`。包壳平均等效应力、等效塑性和蠕变应变误差
分别为 `0.000584%`、`0.004427%` 和 `0.003664%`。接触压力三项误差最大为
`0.040336%`，总接触力误差为 `0.004283%`。

包壳 10 个单元的全部 40 个 Gauss 点也与 MOOSE 一一对齐。等效应力、
等效塑性应变和等效蠕变应变的逐点相对 L2 误差分别为 `0.006587%`、
`0.049466%` 和 `0.014778%`，相对绝对峰值误差分别为 `0.013967%`、
`0.080528%` 和 `0.021603%`，最大单点相对误差分别为 `0.013967%`、
`0.086612%` 和 `0.027296%`。节点场、接触压力、平均量和这 9 项积分点指标
现在全部采用并通过统一 `<0.1%` 门槛。

M4.1 将同一非匹配 PCMI 改为有限应变。MOOSE 只显式设置
`strain = FINITE`，因此采用默认的增量有限应变、Taylor 分解和开启的有限
应变历史旋转；fuelsim 使用相同算法路径。
温度、径向位移、轴向位移和接触压力三项误差的最大值分别为
`0.04262%`、`0.23659%`、`0.06706%` 和 `0.09192%`。40 个包壳积分点的
等效应力、等效塑性应变和等效蠕变应变三项误差最大值分别为
`0.03237%`、`0.28277%` 和 `0.18378%`；全部低于统一的 `0.5%` 门槛。
M4.2 的 10 GPa follower-pressure 对比中，径向和轴向位移的三项全场误差
分别不超过 `2.71e-10%` 和 `8.46e-10%`。
M4.3 的畸变四单元、100 步非共轴路径达到约 `25.5°`，并同时施加当前构形
压力和分量牵引。积分点历史先按每个单元的参考 RZ 体积平均，再逐单元、
逐时间步比较，共覆盖 400 个单元—时间行。节点场和两个等效历史量的三项
误差、张量历史的 L2/峰值误差均低于 `0.5%`。MOOSE 参考补齐倾斜内表面
压力的轴向法向分量后，应力相对 L2 从 `0.004845%` 降到 `0.000525%`；
应力、弹性和总非弹性的最大逐点相对误差降为 `0.5324%`、`0.5324%` 和
`0.4467%`，仅前两项保留显式 `0.6%` qualified 门槛，不使用分母下限。
单单元全位移控制材料对标的张量最大逐点误差低于 `3.4e-7%`，因此没有为
降低对标误差而修改 fuelsim 生产本构或压力离散。纯蠕变隔离还在 MOOSE 的
900 个节点—时间状态上重放生产材料事务：共享状态应力相对 L2 为
`5.15e-8%`，最大逐点误差为 `0.000602%`；自由力学残量相对局部内力尺度
不超过 `0.001419%`。该证据把原 `3.6755%` qualified 点定位为独立平衡路径
在低应力过零处的放大，而不是可复现的本构错误。

M5.7 在同一个 150 节点瞬态有限应变例题中同时激活热接触、Coulomb 摩擦大
滑移、包壳耦合塑性—蠕变、随时间变化的热源和内外压以及历史感知自适应时间
步。MOOSE 参考由一个 MPI 进程和一个线程生成，节点场、接触压力、材料平均量
和 224 个包壳积分点的全部三项误差均低于统一 `0.5%` 门槛。独立的 fuelsim
并行等价性 benchmark 固定所有数值库为单线程，并使用分布式 MUMPS 直接求解器。
两个进程保持 97 个接受步，最大应力和塑性应变绝对差为
`0.1809917 Pa` 和 `3.72567e-12`。四进程会因非线性迭代数跨过自适应控制器
阈值而采用 97 或 98 个接受步；较保守的 98 步路径仍没有时间误差拒绝，最终
温度、位移、应变历史和应力最大绝对差分别为 `3.2460e-4 K`、`1.4632e-11 m`、
`2.9492e-9` 和 `242.35 Pa`，低于对应的 `1e-3 K`、`1e-10 m`、`1e-8` 和
`1e3 Pa` 门槛。接触粘滑布尔状态逐位一致。这是小规模算例的 1、2、4 进程
终态数值等价证据。采用固定核心、单线程数值库和分布式 MUMPS 的受控计时中，
1、2、4 进程的 97 步预热中位数分别为 `17.23 s`、`20.04 s` 和 `18.32 s`；
该小网格没有并行加速，不能据此外推一般扩展性。该长路径不纳入默认 CTest；
手动复现命令、完整首次运行、预热样本和 97/98 步路径边界见
`benchmarks/README.md`。

## 求解器性能

2026-09-09：轴对称手动基准现通过完整生产输入卡运行，包含 23,010 和
45,630 个自由度两档 CAX4T 稳态热弹性接触模型。原 C++ 专用入口已删除。
当前 Abaqus 精度与速度证据见
[轴对称性能验证](verification/abaqus/rz_performance/README.md)。
中等规模采用完整牛顿更新和载荷预测后，全部规定相对误差小于 0.01%，单核外部时间均值
为 22.44 秒（ADlite 0.2.3），Abaqus 为 41.82 秒，耗时减少 46.33%；这是同一主机上
WSL2 与 Windows 的对照测量。

```bash
./build/fuelsim -i verification/fuelsim/steady_rz_performance_medium_timing.fsi
./build/fuelsim -i verification/fuelsim/steady_rz_performance_large_timing.fsi
```

下面的 Quad4、MOOSE 和预条件器数值均为旧程序的历史记录，不是上述 CAX4T
输入的当前测量。

固定 CPU 和所有数值库线程数为 1 后，默认 1,584 DOF 算例的五次运行中位数
为 fuelsim `2.00 s`、MOOSE `3.21 s`，fuelsim 约快 `1.61` 倍。与提交
`319e173` 的同机配对基线相比，对象复用使 fuelsim 从 `2.23 s` 降至
`1.95 s`，墙钟时间缩短 `12.6%`。

旧程序另有不纳入 CTest 的 23,010 DOF、20 载荷步手动基准。

两次单核运行的均值为 fuelsim `40.45 s`、MOOSE `54.97 s`，fuelsim 约快
`1.36` 倍。完整网格、命令、观测范围和计时口径见
`benchmarks/README.md`。

旧手动入口曾经还覆盖 45,630 自由度网格以及 GMRES 配合块 Jacobi、温度—
力学场分裂或 HYPRE。两个 MPI 进程固定在 CPU 0 和 1 的最新测量中，未缩放
HYPRE 完成两个网格的 20 步，但预热时间分别为直接 MUMPS 的 `8.72` 倍和
`3.96` 倍；块 Jacobi 与场分裂没有通过 23,010 自由度的两步筛查。验证矩阵
因此把该证据列为 `measured`，而不是一般迭代缩放结论；完整首轮/预热数据、
失败残量和输入选择指导见 `benchmarks/README.md` 与 `docs/input-card.md`。
在相同 23,010 自由度网格、单进程 CPU 0、直接求解并关闭 MOOSE 文件输出的
最新预热墙钟中，fuelsim 为 `27.04 s`，MOOSE 为 `46.13 s`；这个 `1.71`
比值也只适用于该受控工况。

## 当前边界

一般 Line2 接触允许非匹配分段并支持圆柱侧面、水平端面和斜面，但每侧必须
是一条不分叉的开放边链，secondary 投影必须被 primary 完整覆盖。机械接触
已在当前构形上搜索完整 primary 链并动态转移唯一段所有权；滑出整条链的状态
仍会显式拒绝。当前不支持 mortar 或位移惯性。M5.7 已在规定的 0.6 mm 滑移
路径上直接同时激活 Coulomb 摩擦、机械跨段搜索和热接触动态所有权，并通过
MOOSE 对标及 1、2、4 进程等价性；该证据只适用于规定网格、载荷和接触参数，
不能外推为任意大滑移摩擦路径。
有限应变已支持 MOOSE 默认增量 Taylor 应变、Rashid 转动、历史张量客观
旋转和当前构形内力；压力为当前构形 follower load，分量 traction 默认使用
参考构形，也可在有限应变区域选择当前构形表面测度，方向固定为全局 R
或 Z。这里的增量材料路径属于瞬态问题；稳态有限应变从 `F_old=I` 对总变形
做单次 Taylor 更新，不累计载荷步历史，主方向旋转的路径问题应使用瞬态。
M4.3 已鉴定畸变四单元、100 步、最大约 `25.5°` 且含压力/当前分量
牵引的规定非共轴反向路径；任意转角、任意加载路径和一般网格仍不能由这一
算例外推。MOOSE 只旋转其总非弹性张量，fuelsim 分别旋转塑性和蠕变张量，
因此分机制张量由
局部客观性测试约束，对 MOOSE 只比较客观总量。多 rank 下网格、问题几何和
committed 状态仍在各 rank 复制；PETSc 回调试探态使用按贡献索引构造的稀疏
影子自由度集合。M5.7 的 1、2、4 进程结果已经数值等价，但没有证明运行时间
加速、进程常驻内存缩减或一般并行扩展性。

M2.2 只提供与具体材料无关的 Norton 幂律和线性硬化 J2 算法；其参数支持
相对参考温度的线性 AD 活跃斜率，但不是标定材料关联。暂不实现辐照、燃耗、
孔隙率或应力相关的真实燃料/包壳经验关联，也不支持裂变气体、燃料重定位
或开裂。后续真实模型
必须在当前状态事务、局部 AD Jacobian 和独立 MOOSE 回归基础上逐项加入。
完整的适用边界和 M4.3 的 qualified 门槛见
[工程验证矩阵](docs/verification.md)。
本阶段数值基础和有限应变的算法、输入键及收敛证据见
[M4 说明](docs/m4.md)。
